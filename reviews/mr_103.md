# 🔍 Review MR !103 — wallet-core

**Tổng quan:** MR có nhiều vấn đề nghiêm trọng cần fix trước khi merge.

---

## ⚠️ Cảnh báo tổng thể

- **Template MR thiếu:** Không có Why (impact/severity rõ ràng), không có Verification (test evidence), không có Risk/rollback plan.
- **MR thiếu code:** Description nêu 4 thứ (drop column, đổi error code -5009, thêm flag new_wallet, fix bug tính phí) nhưng diff chỉ có 2 file. Các thay đổi còn lại đâu?

---

## 🔴 Critical — PHẢI FIX TRƯỚC KHI MERGE

### 1. [RC-11] Migration không reversible — DROP COLUMN
**File:** `migrations/0042_drop_balance_old.sql`  
**Vấn đề:** DROP COLUMN là breaking change không thể rollback. Quy trình đúng: `ADD COLUMN → USE → DROP`.  
**Fix:** Tách migration thành 3 bước: (1) Add column mới nếu cần, (2) Deploy code dùng column mới, (3) Drop column cũ sau khi confirm không còn dùng.

### 2. [RC-8] Breaking change thiếu rollout strategy
**Mô tả:** Đổi error code -5009 (theo description) nhưng không thấy code trong diff.  
**Vấn đề:** Đổi error code là breaking change — ai consume? Có notify + update đồng thời?  
**Fix:** Nếu thực sự đổi error code, cần backward-compatible hoặc có migration plan rõ ràng.

### 3. [RC-14] Bugfix không có regression test
**File:** `internal/wallet/fee.go:33`  
**Vấn đề:** Logic thay đổi từ `&&` (AND) thành `||` (OR) — thay đổi hoàn toàn behavior. Không thấy test case cover bug này.  
**Fix:** Thêm unit test chứng minh bug đã được fix (vd: test case với amount <= 0 nhưng tier == VIP phải tính phí đúng).

### 4. [RC-15] Dấu hiệu "deploy Friday trước Tết"
**Mô tả:** Description ghi "Cần merge gấp deploy Friday trước Tết"  
**Vấn đề:** Deadline-driven deploy không có rollback automation + on-call standby → rủi ro cao.  
**Fix:** Nếu vẫn merge, cần đảm bảo có CI/CD rollback tự động và on-call ready.

---

## 🟡 Important

### 5. [IM-4] Migration reversible
**Fix như #1:** Tách add → use → remove.

### 6. [IM-1] SQL change cần EXPLAIN ANALYZE
**File:** `migrations/0042_drop_balance_old.sql`  
**Fix:** Thêm EXPLAIN ANALYZE để xác nhận migration không lock table quá lâu.

---

## 🟢 Tốt

- Code thay đổi nhỏ, dễ review phần fee.go
- Tên migration rõ ràng

---

## ❓ Cần author giải thích

1. Các thay đổi còn lại (đổi error code -5009, thêm flag new_wallet) đâu? Diff thiếu phần này.
2. Logic `amount > 0 || tier == TierVIP` — tại sao thay đổi từ AND sang OR? Mong muốn behavior gì?

---

**Kết luận:** ❌ **BLOCK MERGE** — cần fix RC-11, RC-14, và giải thích các thay đổi còn thiếu.